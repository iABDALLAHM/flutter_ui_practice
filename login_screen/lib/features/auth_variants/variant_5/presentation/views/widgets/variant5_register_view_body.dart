import 'package:flutter/material.dart';

class Variant5RegisterViewBody extends StatelessWidget {
  const Variant5RegisterViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
