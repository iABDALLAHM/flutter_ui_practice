import 'package:flutter/material.dart';

class NowShowingViewBody extends StatelessWidget {
  const NowShowingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [

      ],
    );
  }
}
