import 'package:flutter/material.dart';

class CastingViewBody extends StatelessWidget {
  const CastingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
